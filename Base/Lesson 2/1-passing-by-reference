//Task 1
9, 14 (потому что number - простой тип данных, мы сначала в него записали значение t.y, а потом изменили значение этой переменной. Но значение ключа объекта это не перезапишет).

//Task 2
5 (опять же, поскольку мы сначала записали значение простого типа данных, оно в ячейке памяти сохранилось, и все последующие манипуляции с массивом его уже не меняют).

//Task 3
10, 9 (то же самое, меняется только свойство объекта, простой тип данных - нет).

//Task 4
name: "Kolya", number: 67, 67;

//Task 5
undefined из-за object2.str.number = object2.str.number; такой ключ не объявлен в объекте.
